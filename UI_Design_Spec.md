# MapproxyServer GUI 布局与交互设计规范

## 1. 概述
本设计规范描述了 MapproxyServer 启动器（Launcher）的重构后用户界面布局、模块划分及交互逻辑。旨在提供一个直观、响应式且安全的操作环境。

## 2. 窗口布局设计

### 2.1 整体尺寸
- **默认尺寸**: 1200px (宽) x 800px (高)
- **最小尺寸**: 320px (宽) x 480px (高)
- **布局模式**: 基于 Tkinter Grid 的响应式布局

### 2.2 区域划分 (Grid System)
主窗口分为 5 个主要行区域：

| 行号 (Row) | 区域名称 | 权重 (Weight) | 说明 |
| :--- | :--- | :--- | :--- |
| **0** | **运行环境区** | 0 (固定高度) | 独占一行，Python 解释器选择与管理 |
| **1** | **服务配置区** | 0 (固定高度) | 独占一行，端口、Host 及高级配置 |
| **2** | **运行状态区** | 0 (固定高度) | 状态显示、URL 及垂直排列的操作控制 |
| **3** | **图层列表区** | 1 (可伸缩) | 展示已配置的图层信息 |
| **4** | **日志控制台** | 1 (可伸缩) | 实时日志输出与控制 |

### 2.3 响应式布局策略
- **Grid 权重管理**：
  - 根容器列权重设置为 1，确保内容水平充满窗口。
  - 图层列表和日志区域行权重设置为 1，确保垂直方向自适应伸缩。
- **滚动视图容器**：
  - 引入 `Canvas` + `Scrollbar` 包装整个主界面，支持最小 **320px** 宽度的设备访问，防止内容在小屏幕下被截断。
- **自适应字体与尺寸**：
  - **移动端/小屏 (< 768px)**：基础字号 **9pt**，按钮内边距减少。
  - **标准桌面 (768px - 1200px)**：基础字号 **10pt** (默认)。
  - **大屏显示 (>= 1200px)**：基础字号 **11pt**，按钮内边距增加。
  - 通过监听窗口 `<Configure>` 事件动态调整全局样式。
- **最小尺寸限制**：
  - 窗口最小尺寸调整为 **320x480**，以覆盖移动端模拟测试场景。

---

## 3. 模块详细设计

### 3.1 运行环境 (Row 0)
- **容器样式**: Card (Groove border)
- **布局**: 横向排列 (Grid/Pack)
- **组件**:
  - `Label`: "Python 解释器:"
  - `Combobox`: 解释器选择 (只读, 自动伸缩)
  - `Button`: "刷新" (触发扫描)

### 3.2 服务配置与控制 (Row 1)
- **容器样式**: Card (Groove border)
- **布局**: 基于 Grid 的 3 行结构
- **Line 1 (基础配置)**:
  - `Entry`: 端口
  - `Entry`: 线程数 (新增)
  - `Checkbutton`: 允许外部访问
  - `Label`: Host 显示
  - `Button`: 高级设置
  - `Button`: 保存配置
- **Line 2 (服务控制)**:
  - **布局**: 紧凑型按钮组 (Horizontal)
  - `Button`: 启动服务 (左侧, 绿色/默认)
  - `Button`: 停止服务 (右侧, 红色/禁用)
  - **间距**: 按钮之间 5px 间距
- **Line 3 (工作目录)**:
  - `Label`: "工作目录:"
  - `Entry`: 完整路径显示 (只读, Consolas 字体)
  - `Button`: 打开目录

### 3.3 运行状态 (Row 2)
- **容器样式**: Card (Highlight)
- **布局**: 仅保留状态显示头
- **组件**:
  - **状态指示器**: 图标 + 文字
  - **服务地址**: URL (只读) + 复制按钮
  - **浏览器**: 快捷打开按钮
- **说明**: 原有的控制按钮已迁移至服务配置模块。

### 3.4 样式规范 (Button & Typography)
- **字体**: Microsoft YaHei
- **字号**: ≥ 14px (pixel-based)
- **按钮尺寸**:
  - 高度: 约 32px
  - 内边距: 水平 8px, 垂直 4px
  - 最小宽度: 目标 80px (通过 Padding 和文字控制)
- **响应式**:
  - 统一字号，但在小屏下保持可读性。

### 3.5 图层列表 (Row 3)
- **容器样式**: Card (Groove border)
- **组件**:
  - `Treeview`: 多列列表 (Name, Format, Title)
  - **高度**: 默认显示 4 行 (限制在可视区域 30% 以内)
  - **交互**: 单击单元格复制内容, Hover 高亮
- **滚动条**: 垂直滚动条 (自动显示)

### 3.5 运行日志 (Row 4)
- **工具栏**:
  - `Combobox`: 日志级别筛选 (DEBUG/INFO/WARN/ERROR)
  - `Checkbutton`: "自动滚动"
  - `Button`: "导出日志" (Save As)
  - `Button`: "清空日志"
- **内容区**:
  - `ScrolledText`: 彩色日志输出 (基于 Tag)

---

## 4. 交互逻辑与状态管理

### 4.1 状态机 (State Machine)
界面控件状态严格跟随服务运行状态：

| 服务状态 | 配置控件 | 启动按钮 | 停止按钮 | 状态指示器 |
| :--- | :--- | :--- | :--- | :--- |
| **Stopped** | Normal (可用) | Normal | Disabled | 灰色/红色 |
| **Starting** | Disabled (禁用) | Disabled | Normal | 橙色 |
| **Running** | Disabled (禁用) | Disabled | Normal | 绿色 |

### 4.2 关键交互规则
1.  **配置锁定**:
    - 当服务启动 (`is_running=True`) 时，调用 `update_controls_state(True)`。
    - 禁用：Python 选择、刷新、端口输入、外部访问开关、保存按钮、高级设置按钮。
    - 防止用户在运行时修改配置导致状态不一致。

2.  **输入验证**:
    - **端口**: 仅允许 1-65535 整数。输入错误时输入框变红，保存按钮禁用。
    - **线程数**: 仅允许 1-32 整数。输入错误时输入框变红，保存按钮禁用。
    - **路径**: 启动前检查 Python 路径和脚本路径是否存在。

3.  **日志管理**:
    - **缓冲机制**: 日志先存入内存 Buffer (`log_buffer`)。
    - **视图刷新**: 更改日志级别时，不重新读取文件，而是从 Buffer 重新渲染 (`refresh_log_view`)，实现无闪烁过滤。

4.  **快捷键**:
    - `Ctrl+L`: 滚动日志到底部。

### 4.3 反馈机制
- **Toast**: 复制地址、保存配置成功时显示轻量级悬浮提示。
- **Dialog**: 错误或关键确认（如停止服务）使用模态对话框。

## 5. 视觉规范
- **字体**: 优先使用 "Microsoft YaHei" (UI) 和 "Consolas" (Log/Code).
- **间距**: 模块间距 15px，内部 Padding 10px。
- **颜色**:
  - Success: Green
  - Warning: Orange
  - Error: Red
  - Text: Black / Gray (Secondary)
